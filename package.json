{
  "name": "json-schema",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild --outdir=dist src/compile.ts",
    "test": "npx esbuild --platform=node --format=cjs --bundle --outfile=test/build/index.js test/index.ts && npx uvu test/build/",
    "test:update": "IS_UPDATE=1 npm run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.16",
    "fast-glob": "^3.2.11",
    "uvu": "0.5.3"
  }
}
